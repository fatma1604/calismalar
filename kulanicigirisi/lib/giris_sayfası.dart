import 'package:flutter/material.dart';

class GirisSayfas extends StatelessWidget {
  const GirisSayfas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("giris sayfası")),
    );
  }
}

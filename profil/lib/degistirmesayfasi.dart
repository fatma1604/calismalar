import 'package:flutter/material.dart';

class Degistirmesayfasi extends StatelessWidget {
const Degistirmesayfasi({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container(
      child: Center(child: Text("değiştirme")),
    );
  }
}